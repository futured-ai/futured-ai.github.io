<div class="card judges-section">
  <h1 class="pffbold">{{ include.title }}</h1>
  <div class="judges-grid">
    {% if include.subtitle %}
      <div class="card container">
        <p class="pfflight">{{ include.subtitle }}</p>
      </div>
    {% endif %}
    {% for person in include.people %}
    <div class="judge-card card">
      <div class="judge-header">
        <img class="judge-photo" src="{{ person.image }}" alt="{{ person.name }}">
        <div class="judge-info">
          <h3 class="judge-name">{{ person.name }}</h3>
          <p class="judge-title">{{ person.title }}</p>
        </div>
      </div>
      <p class="judge-bio">{{ person.bio }}</p>
    </div>
    {% endfor %}
  </div>
</div>
